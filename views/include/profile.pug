script(src="/javascript/profile.js")
div(class="profile-container bg-white mr-3 d-flex flex-column")
    form(class="d-flex p-3 flex-row justify-content-center align-items-center" action="/users/update/image" method="POST" enctype="multipart/form-data" id ="formContainer")
      input(type="file" name="avatar" class="d-none" id="inputAvatar")
      img(src=currentUser.avatar class="img-fluid" id="imageProfile")
    div(class="d-flex flex-row justify-content-center align-items-center my-3")
        p= `${ currentUser.username } (${ currentUser.local.email })`
    div(class="text-center d-flex flex-row justify-content-center align-items-center my-3")
      div(class="flex-fill")
        p
          strong Tweet
          br
          span(class="text-primary")= tweets && tweets.length ? tweets.length : "-"
      div(class="flex-fill")
        p
          strong Following
          br
          span(class="text-primary") -